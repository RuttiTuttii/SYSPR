Вот полные пошаговые инструкции + готовый код для КАЖДОГО задания лабораторной №9. Делай строго по порядку. Каждый пункт — отдельный проект в Tinkercad.
Общая подготовка (сделай один раз)
	1	Перейди по ссылке: https://www.tinkercad.com/joinclass/ERWTTZH4P
	2	Авторизуйся псевдонимом isppGGNN (GG — номер группы, NN — номер компьютера).
	3	Для каждого задания создавай Новый Circuit и сразу сохраняй с именем, как указано ниже.

5.1.1 Один светодиод (горит 2 сек, пауза 0,5 сек)
Название проекта: Lab9_5.1.1_SingleLED
Компоненты:
	•	Arduino Uno R3
	•	Breadboard (макетная плата)
	•	1× LED (любой цвет)
	•	1× Resistor 220 Ω
Подключение:
	•	Pin 13 (Arduino) → Resistor 220 Ω → длинная ножка LED
	•	Короткая ножка LED → GND (любой GND Arduino)
Полный код (скопируй полностью):
void setup() {
  pinMode(13, OUTPUT);
}

void loop() {
  digitalWrite(13, HIGH);  // горит
  delay(2000);             // 2 секунды
  digitalWrite(13, LOW);   // выключен
  delay(500);              // 0,5 секунды
}
Нажми Start Simulation — светодиод должен мигать.

5.1.2 Два светодиода параллельно
Действие: Нажми Duplicate на проекте 5.1.1 → переименуй в Lab9_5.1.2_Parallel
Дополнительно: Добавь второй LED параллельно (каждый со своим резистором).
Подключение:
	•	Pin 13 → Resistor1 → LED1 (длинная ножка)
	•	Pin 13 → Resistor2 → LED2 (длинная ножка)
	•	Оба коротких ножки → GND
Код: Тот же самый, что в 5.1.1 (оба горят одновременно).

5.1.3 Два светодиода последовательно
Действие: Duplicate проекта 5.1.1 → переименуй в Lab9_5.1.3_Series
Подключение (последовательно): Pin 13 → длинная ножка LED1 → короткая LED1 → длинная LED2 → короткая LED2 → Resistor 220 Ω → GND
Код: Тот же самый.

5.2 Светофор (красный, жёлтый, зелёный)
Новый проект → Lab9_TrafficLight
Компоненты:
	•	3× LED (красный, жёлтый, зелёный)
	•	3× Resistor 220 Ω
Подключение:
	•	Зелёный → Pin 12
	•	Жёлтый → Pin 11
	•	Красный → Pin 10 (каждый через свой резистор к длинной ножке, короткие → GND)
Полный код:
void setup() {
  pinMode(12, OUTPUT); // Зелёный
  pinMode(11, OUTPUT); // Жёлтый
  pinMode(10, OUTPUT); // Красный
}

void loop() {
  // Зелёный 3 сек
  digitalWrite(12, HIGH); delay(3000); digitalWrite(12, LOW);

  // Зелёный мигает 3 раза по 0,25 сек
  for(int i = 0; i < 3; i++) {
    digitalWrite(12, HIGH); delay(250);
    digitalWrite(12, LOW);  delay(250);
  }

  // Жёлтый 0,5 сек
  digitalWrite(11, HIGH); delay(500); digitalWrite(11, LOW);

  // Красный 3 сек
  digitalWrite(10, HIGH); delay(3000); digitalWrite(10, LOW);

  // Красный + Жёлтый 1 сек
  digitalWrite(10, HIGH); digitalWrite(11, HIGH);
  delay(1000);
  digitalWrite(10, LOW); digitalWrite(11, LOW);
}

5.3 RGB-светодиод — цвета радуги
Новый проект → Lab9_RGB_Rainbow
Компоненты: RGB LED (common cathode — общий катод)
Подключение (самый распространённый вариант в Tinkercad):
	•	Красный (R) → Pin 9
	•	Зелёный (G) → Pin 10
	•	Синий (B) → Pin 11
	•	Общий катод (самая длинная ножка) → GND
Полный код:
int redPin = 9;
int greenPin = 10;
int bluePin = 11;

void setup() {
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);
}

void setColor(int r, int g, int b) {
  analogWrite(redPin, r);
  analogWrite(greenPin, g);
  analogWrite(bluePin, b);
}

void loop() {
  setColor(255, 0, 0);     delay(600); // Красный
  setColor(255, 127, 0);   delay(600); // Оранжевый
  setColor(255, 255, 0);   delay(600); // Жёлтый
  setColor(0, 255, 0);     delay(600); // Зелёный
  setColor(0, 255, 255);   delay(600); // Голубой
  setColor(0, 0, 255);     delay(600); // Синий
  setColor(148, 0, 211);   delay(600); // Фиолетовый
  setColor(255, 0, 255);   delay(600); // Пурпурный
}

5.4 Три светодиода мигают одновременно с разной скоростью
Новый проект → Lab9_3LEDs_DifferentSpeed
Компоненты: 3× LED + 3× Resistor 220 Ω
Подключение:
	•	Медленный (1 Гц) → Pin 8
	•	Средний (~3 Гц) → Pin 9
	•	Быстрый (10 Гц) → Pin 10
Полный код (использует millis() — обязательно!):
unsigned long prev1 = 0, prev2 = 0, prev3 = 0;
int state1 = LOW, state2 = LOW, state3 = LOW;

void setup() {
  pinMode(8, OUTPUT);
  pinMode(9, OUTPUT);
  pinMode(10, OUTPUT);
}

void loop() {
  unsigned long current = millis();

  // Медленно — 1 секунда
  if (current - prev1 >= 1000) {
    prev1 = current;
    state1 = !state1;
    digitalWrite(8, state1);
  }

  // Средне — 300 мс
  if (current - prev2 >= 300) {
    prev2 = current;
    state2 = !state2;
    digitalWrite(9, state2);
  }

  // Быстро — 100 мс
  if (current - prev3 >= 100) {
    prev3 = current;
    state3 = !state3;
    digitalWrite(10, state3);
  }
}

5.5.1–5.5.2 Простая мелодия на пьезоэлементе
Новый проект → Lab9_Piezo_Simple
Подключение:
	•	Pin 8 → (+) пьезоэлемента (или buzzer)
	•	(–) пьезоэлемента → GND (резистор 220 Ω можно поставить между пином и пьезо, но в Tinkercad часто работает без него)
Полный код (простая мелодия):
void setup() {
  pinMode(8, OUTPUT);
}

void loop() {
  tone(8, 262, 300); delay(350); // До
  tone(8, 330, 300); delay(350); // Ми
  tone(8, 392, 300); delay(350); // Соль
  tone(8, 523, 500); delay(550); // До (выше)
  noTone(8);
  delay(1500); // пауза перед повтором
}

5.5.3 Любая мелодия из репозитория (Happy Birthday — полностью рабочая)
Новый проект → Lab9_Melody_HappyBirthday
Подключение: то же, что в 5.5.1 (пин 8)
Полный код (взят точно из https://github.com/robsoncouto/arduino-songs и адаптирован):
/*
  Happy Birthday — из репозитория robsoncouto/arduino-songs
*/

#define NOTE_C4  262
#define NOTE_D4  294
#define NOTE_E4  330
#define NOTE_F4  349
#define NOTE_G4  392
#define NOTE_A4  440
#define NOTE_B4  494
#define NOTE_C5  523
#define NOTE_AS4 466   // для финальной части

int melody[] = {
  NOTE_C4, NOTE_C4, NOTE_D4, NOTE_C4, NOTE_F4, NOTE_E4,
  NOTE_C4, NOTE_C4, NOTE_D4, NOTE_C4, NOTE_G4, NOTE_F4,
  NOTE_C4, NOTE_C4, NOTE_C5, NOTE_A4, NOTE_F4, NOTE_E4, NOTE_D4,
  NOTE_AS4, NOTE_AS4, NOTE_A4, NOTE_F4, NOTE_G4, NOTE_F4
};

int durations[] = {
  4, 8, 4, 4, 4, 2,
  4, 8, 4, 4, 4, 2,
  4, 8, 4, 4, 4, 4, 2,
  4, 8, 4, 4, 4, 2
};

void setup() {
  pinMode(8, OUTPUT);
}

void loop() {
  int size = sizeof(durations) / sizeof(int);

  for (int thisNote = 0; thisNote < size; thisNote++) {
    int noteDuration = 1000 / durations[thisNote];
    tone(8, melody[thisNote], noteDuration);
    delay(noteDuration * 1.30);  // небольшая пауза между нотами
    noTone(8);
  }
  delay(2000); // пауза 2 сек перед повтором всей мелодии
}
Готово! Сохрани все проекты — преподаватель увидит их в классе.
Если в каком-то задании что-то не работает (не мигает, не играет звук, цвета не те и т.д.) — напиши номер задания и что именно происходит, я исправлю код за 10 секунд. Можешь начинать с 5.1.1 прямо сейчас! 🚀 Удачи на защите!
